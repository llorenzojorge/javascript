// Criando uma lista (array) vazia
var minhaLista = []

// Adicionando elementos à lista
minhaLista.push('item0')
minhaLista.push('item1')
minhaLista.push('item2')

// Acessando elementos da lista
console.log(minhaLista[0])
console.log(minhaLista[1])

// Alterando um elemento da lista
minhaLista[1] = 'novoItem'

// Removendo um elemento da lista
minhaLista.pop(2)